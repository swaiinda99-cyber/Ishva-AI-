/**
 * Ishva AI Application Entry Point
 * Ekka Technologies
 */

import { initializeUI } from './components/ui.js';

document.addEventListener('DOMContentLoaded', () => {
  initializeUI();
  console.log('⚡ Ishva AI Multi-Agent Platform Initialized.');
});
